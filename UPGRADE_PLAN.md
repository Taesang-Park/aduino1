# 앱 업그레이드 계획서

**작성일**: 2025-01-08
**버전**: 1.0 → 2.0
**목적**: 시간 간격 기반 수분 섭취 목표 시스템 추가

---

## 📋 요구사항 분석

### 1. 시간 간격 기반 목표 시스템
- 사용자 설정: 하루 총 목표량, 시간 간격 (1h/2h/3h/커스텀)
- 자동 계산:
  - `timesPerDay = 16시간 / 선택한 간격`
  - `goalPerInterval = dailyGoal / timesPerDay`
- 각 구간별 섭취량 추적 및 자동 초기화

### 2. RGB LED 제어 (C:x 명령)
- 달성률에 따른 LED 색상:
  - 0-50%: `C:0` (Red)
  - 50-100%: `C:1` (Yellow)
  - 100%↑: `C:2` (Blue)

### 3. 로그 기능 개선
- 자동 기록 (기존 D:값)
- 수동 추가 기능
- 잘못된 기록 삭제
- 히스토리 화면

### 4. 통계 그래프
- 시간대별 섭취량 바 차트
- 하루 총 섭취량
- 구간별 목표 vs 실제 그래프

---

## 🏗️ 전체 구조 설계

### 아키텍처 (기존 유지)
```
Presentation Layer (UI + ViewModel)
    ↓
Domain Layer (비즈니스 로직)
    ↓
Data Layer (Repository + Database + Bluetooth)
```

### 새로운 화면 구조
```
MainActivity
  └─ Navigation
      ├─ HomeScreen (기존 - 개선)
      ├─ SettingsScreen (신규)
      ├─ HistoryScreen (신규)
      └─ StatisticsScreen (신규)
```

---

## 📁 새로 추가할 파일 목록

### Domain Layer
```
domain/model/
  ├─ HydrationInterval.kt          # 시간 구간 모델
  ├─ HydrationSettings.kt          # 설정 모델
  ├─ IntervalStatus.kt             # 구간 상태 (Enum)
  └─ LedColorCommand.kt            # LED 명령 (Enum)

domain/calculator/
  └─ IntervalCalculator.kt         # 구간 계산 로직
```

### Data Layer
```
data/local/entity/
  └─ HydrationSettingsEntity.kt    # 설정 저장 Entity

data/local/dao/
  └─ SettingsDao.kt                # 설정 DAO

data/repository/
  └─ SettingsRepository.kt         # 설정 Repository
```

### Presentation Layer
```
presentation/settings/
  ├─ SettingsViewModel.kt          # 설정 ViewModel
  └─ SettingsScreen.kt             # 설정 화면

presentation/history/
  ├─ HistoryViewModel.kt           # 히스토리 ViewModel
  └─ HistoryScreen.kt              # 히스토리 화면

presentation/statistics/
  ├─ StatisticsViewModel.kt        # 통계 ViewModel
  └─ StatisticsScreen.kt           # 통계 화면

presentation/navigation/
  └─ NavGraph.kt                   # Navigation 구성
```

---

## 🔧 수정할 기존 파일

### Arduino
```
arduino/water_monitor.ino
  + C:x 명령 파싱 및 RGB LED 제어 로직 추가
```

### Data Layer
```
data/local/database/WaterDatabase.kt
  + SettingsDao 추가
  + 데이터베이스 버전 업그레이드 (1 → 2)

data/repository/WaterRepository.kt
  + 수동 추가 기능
  + 삭제 기능
  + 시간대별 조회 기능
```

### Presentation Layer
```
presentation/bluetooth/BluetoothManager.kt
  + sendColorCommand(color: LedColorCommand) 함수 추가

presentation/home/HomeViewModel.kt
  + 시간 구간 로직 통합
  + LED 상태 자동 전송
  + 설정값 연동

presentation/home/HomeScreen.kt
  + 구간 정보 표시 UI 추가
  + Navigation 연동
```

### UI
```
MainActivity.kt
  + NavHost 추가
```

---

## 💾 데이터베이스 스키마

### 기존 (유지)
```sql
water_records (
  id LONG PRIMARY KEY,
  amount INT,
  timestamp LONG,
  date STRING
)
```

### 신규
```sql
hydration_settings (
  id LONG PRIMARY KEY,
  dailyGoal INT,           -- 하루 목표량 (ml)
  intervalHours FLOAT,     -- 시간 간격 (시간)
  wakingHours INT,         -- 깨어있는 시간 (기본 16)
  startTime LONG           -- 하루 시작 시간 (타임스탬프)
)
```

---

## 🎨 UI/UX 설계

### 1. HomeScreen (개선)
```
┌─────────────────────────────────┐
│  물 섭취량 모니터          ⚙️📊📋│
├─────────────────────────────────┤
│  [현재 구간 정보 카드]           │
│  ┌───────────────────────────┐  │
│  │ 🕐 2번째 구간 (14:00-16:00)│  │
│  │ 목표: 250ml               │  │
│  │ 현재: 180ml (72%)         │  │
│  │ 🟡 Yellow (보통)          │  │
│  │ ⏱️ 남은 시간: 45분        │  │
│  └───────────────────────────┘  │
│                                  │
│  [블루투스 연결] (기존)          │
│  [현재 무게] (기존)              │
│  [오늘 총 섭취량] (기존)         │
└─────────────────────────────────┘
```

### 2. SettingsScreen (신규)
```
┌─────────────────────────────────┐
│  ← 설정                         │
├─────────────────────────────────┤
│  하루 목표량                     │
│  [2000] ml                      │
│                                  │
│  시간 간격                       │
│  ○ 1시간마다                    │
│  ● 2시간마다                    │
│  ○ 3시간마다                    │
│  ○ 사용자 설정: [2.5] 시간      │
│                                  │
│  활동 시간                       │
│  [16] 시간                      │
│                                  │
│  [저장]                         │
└─────────────────────────────────┘
```

### 3. HistoryScreen (신규)
```
┌─────────────────────────────────┐
│  ← 히스토리                     │
├─────────────────────────────────┤
│  오늘 (2025-01-08)              │
│                                  │
│  ┌─────────────────────────┐   │
│  │ 14:30  200ml  [자동] 🗑️ │   │
│  └─────────────────────────┘   │
│  ┌─────────────────────────┐   │
│  │ 12:15  150ml  [수동] 🗑️ │   │
│  └─────────────────────────┘   │
│  ┌─────────────────────────┐   │
│  │ 10:00  300ml  [자동] 🗑️ │   │
│  └─────────────────────────┘   │
│                                  │
│  [+ 수동 추가]                  │
└─────────────────────────────────┘
```

### 4. StatisticsScreen (신규)
```
┌─────────────────────────────────┐
│  ← 통계                         │
├─────────────────────────────────┤
│  오늘 총 섭취량                  │
│  1450 / 2000 ml (73%)           │
│                                  │
│  시간대별 섭취량                 │
│  ┌───────────────────────────┐  │
│  │ 300ml ████████            │  │
│  │ 250ml ███████             │  │
│  │ 200ml ██████              │  │
│  │ 150ml ████                │  │
│  │       8  10  12  14  16   │  │
│  └───────────────────────────┘  │
│                                  │
│  구간별 달성률                   │
│  ┌───────────────────────────┐  │
│  │ 1️⃣ ✅ 100%  5️⃣ ⬜ 20%    │  │
│  │ 2️⃣ ✅ 120%  6️⃣ ⬜ 0%     │  │
│  │ 3️⃣ ✅ 80%   7️⃣ ⬜ 0%     │  │
│  │ 4️⃣ ⚠️ 72%   8️⃣ ⬜ 0%     │  │
│  └───────────────────────────┘  │
└─────────────────────────────────┘
```

---

## 🔄 데이터 흐름

### 시간 구간 계산 흐름
```
1. 앱 시작
   ↓
2. SettingsRepository에서 설정 로드
   ↓
3. IntervalCalculator가 현재 구간 계산
   - 현재 시간
   - 시작 시간
   - 간격
   ↓
4. 현재 구간의 섭취량 계산
   ↓
5. 달성률 계산
   ↓
6. LED 색상 결정
   ↓
7. BluetoothManager.sendColorCommand()
   ↓
8. Arduino가 LED 색상 변경
```

### 자동 기록 + LED 업데이트 흐름
```
1. Arduino에서 D:50.0 전송
   ↓
2. BluetoothManager 수신
   ↓
3. HomeViewModel에서 감지
   ↓
4. Repository에 기록 저장
   ↓
5. 현재 구간 섭취량 재계산
   ↓
6. 달성률 업데이트
   ↓
7. LED 색상 재평가
   ↓
8. 필요시 새 색상 명령 전송
   ↓
9. UI 자동 업데이트
```

---

## 🧮 핵심 로직

### 1. 시간 구간 계산
```kotlin
// 예시: 2시간 간격, 16시간 활동
val intervalHours = 2.0
val wakingHours = 16
val timesPerDay = (wakingHours / intervalHours).toInt()  // 8회

val dailyGoal = 2000  // ml
val goalPerInterval = dailyGoal / timesPerDay  // 250ml

// 현재 몇 번째 구간?
val startTime = getStartTimeOfDay()  // 오전 8시
val currentTime = System.currentTimeMillis()
val elapsedHours = (currentTime - startTime) / (1000 * 60 * 60)
val currentInterval = (elapsedHours / intervalHours).toInt() + 1

// 현재 구간의 시작/종료 시간
val intervalStart = startTime + (currentInterval - 1) * intervalHours * 3600000
val intervalEnd = intervalStart + intervalHours * 3600000
```

### 2. 달성률 계산 및 LED 색상 결정
```kotlin
fun calculateLedColor(currentAmount: Int, goalAmount: Int): LedColorCommand {
    val percentage = (currentAmount.toFloat() / goalAmount) * 100

    return when {
        percentage < 50 -> LedColorCommand.RED     // C:0
        percentage < 100 -> LedColorCommand.YELLOW // C:1
        else -> LedColorCommand.BLUE               // C:2
    }
}
```

### 3. 구간 자동 전환
```kotlin
// ViewModel에서 타이머 실행
viewModelScope.launch {
    while (true) {
        delay(60000)  // 1분마다 체크

        val currentTime = System.currentTimeMillis()
        if (currentTime >= currentIntervalEnd) {
            // 다음 구간으로 전환
            moveToNextInterval()
        }
    }
}
```

---

## 📊 통계 그래프 데이터 구조

### 시간대별 섭취량
```kotlin
data class HourlyIntake(
    val hour: Int,        // 0-23
    val amount: Int       // ml
)

// 예시 데이터
[
    HourlyIntake(8, 300),
    HourlyIntake(10, 250),
    HourlyIntake(12, 200),
    ...
]
```

### 구간별 달성률
```kotlin
data class IntervalAchievement(
    val intervalNumber: Int,   // 1-8
    val goal: Int,             // 250ml
    val actual: Int,           // 180ml
    val percentage: Float      // 72%
)
```

---

## 🔌 Bluetooth 명령 확장

### 기존 명령
```
W:250.5   - 현재 무게
D:50.0    - 마신 양
S:READY   - 상태
T         - 영점 조정
R         - 리셋
```

### 신규 명령
```
C:0       - LED Red
C:1       - LED Yellow
C:2       - LED Blue
```

### Arduino 파싱 로직 (추가)
```cpp
if (message.startsWith("C:")) {
    int colorCode = message.substring(2).toInt();
    setRgbLed(colorCode);
}
```

---

## 🎯 구현 우선순위

### Phase 1: 기반 작업
1. Domain 모델 생성
2. IntervalCalculator 구현
3. Database 스키마 확장
4. SettingsRepository 구현

### Phase 2: Bluetooth 확장
1. BluetoothManager에 C:x 명령 추가
2. Arduino 코드 업데이트
3. LED 제어 테스트

### Phase 3: UI 구현
1. Navigation 구성
2. SettingsScreen 구현
3. HomeScreen 개선
4. HistoryScreen 구현
5. StatisticsScreen 구현

### Phase 4: 통합 및 테스트
1. 전체 데이터 흐름 연결
2. 시간 구간 자동 전환 테스트
3. LED 자동 업데이트 테스트
4. UI/UX 최종 점검

---

## 📝 추가 고려사항

### 1. 시간 동기화
- 앱 시작 시간을 설정에서 조정 가능 (예: 오전 8시)
- 자정이 지나면 자동으로 리셋

### 2. 알림 기능 (선택사항)
- 각 구간 시작 시 알림
- 목표 미달성 시 알림

### 3. 데이터 백업
- CSV 내보내기
- 클라우드 동기화 (향후)

### 4. 성능 최적화
- 1분마다 구간 체크 최적화
- Flow 대신 필요시에만 계산

---

## ✅ 완료 체크리스트

### Domain Layer
- [ ] HydrationInterval.kt
- [ ] HydrationSettings.kt
- [ ] IntervalStatus.kt
- [ ] LedColorCommand.kt
- [ ] IntervalCalculator.kt

### Data Layer
- [ ] HydrationSettingsEntity.kt
- [ ] SettingsDao.kt
- [ ] SettingsRepository.kt
- [ ] WaterDatabase 업그레이드
- [ ] WaterRepository 기능 추가

### Presentation Layer
- [ ] BluetoothManager C:x 명령
- [ ] SettingsViewModel.kt
- [ ] SettingsScreen.kt
- [ ] HistoryViewModel.kt
- [ ] HistoryScreen.kt
- [ ] StatisticsViewModel.kt
- [ ] StatisticsScreen.kt
- [ ] HomeViewModel 업그레이드
- [ ] HomeScreen 개선
- [ ] NavGraph.kt

### Arduino
- [ ] C:x 명령 파싱
- [ ] RGB LED 제어

### 테스트
- [ ] 구간 계산 로직 테스트
- [ ] LED 색상 변경 테스트
- [ ] 자동 기록 + LED 업데이트
- [ ] Navigation 테스트
- [ ] 전체 시나리오 테스트

---

**다음 단계**: 각 파일을 순차적으로 구현하겠습니다.
